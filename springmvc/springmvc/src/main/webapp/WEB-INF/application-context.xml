<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
               http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
               http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        Load database credentials from a properties file.
        Keeps passwords out of XML.
    -->
    <context:property-placeholder location="/WEB-INF/db.properties"/>

    <!--
        Scan all packages EXCEPT controllers (those are in servlet-context.xml)
    -->
    <context:component-scan base-package="com.smartcampus">
        <context:exclude-filter type="annotation"
            expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!--
        DataSource - the connection pool.
        Spring's DriverManagerDataSource opens connections on demand.
        In production you would use HikariCP or c3p0 for pooling.
    -->
    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${db.driver}"/>
        <property name="url"             value="${db.url}"/>
        <property name="username"        value="${db.username}"/>
        <property name="password"        value="${db.password}"/>
    </bean>

    <!--
        JdbcTemplate - Spring's helper for running SQL without boilerplate.
        Injects the dataSource defined above.
        All DAO classes receive this via @Autowired.
    -->
    <bean id="jdbcTemplate"
          class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

</beans>
