<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
               http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
               http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
               http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        Enables Spring MVC annotations:
        @Controller, @RequestMapping, @GetMapping, @PostMapping, etc.
    -->
    <mvc:annotation-driven/>

    <!--
        Tells Spring where to scan for @Controller classes.
        Only the controller package - application beans are in application-context.xml
    -->
    <context:component-scan base-package="com.smartcampus.controller"/>

    <!--
        ViewResolver - tells Spring MVC where JSP files are located
        and what extension they use.

        When a controller returns "student/list", Spring will look for:
        /WEB-INF/views/student/list.jsp
    -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!--
        Serve static resources (CSS, JS, images) without going through dispatcher.
        Files in /resources/static/ are served as-is.
    -->
    <mvc:resources mapping="/static/**" location="/resources/static/"/>

</beans>
